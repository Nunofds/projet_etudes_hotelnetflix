
{{ form_start(form) }}
{{ form_widget(form) }}
<div class="container">
    <button class="btn btn-success mb-3">{{ button_label|default('Ajouter') }}</button>
    <a href="{{ path('app_admin_serie_index') }}" class="btn btn-danger mb-3">Retour</a>
</div>
{{ form_end(form) }}



