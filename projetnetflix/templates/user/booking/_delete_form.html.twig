<form id="form-delete" method="post" action="{{ path('app_user_booking_delete', {'id': booking.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cette réservation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booking.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
